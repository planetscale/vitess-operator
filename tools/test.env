# This script sets up the environment for the tests

# Prefer the pinned test binaries over tools supplied by the host.
export PATH="$PWD/tools/_bin:$PATH"

# We default to the latest version of Kind.
export KIND_VERSION="kindest/node:v${KIND_VERSION:-1.34.0}"
